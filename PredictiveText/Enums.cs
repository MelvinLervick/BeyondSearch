namespace PredictiveText
{
    public enum SearchAlgorythms
    {
        Trie = 1,
        FileTrie = 2,
        MemoryTrie = 3
    }

    public enum TreeTypes
    {
        WordPosition = 1,
        Weighted = 2
    }
}